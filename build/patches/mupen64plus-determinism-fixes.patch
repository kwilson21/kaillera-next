diff --git a/mupen64plus-core/src/device/r4300/r4300_core.c b/mupen64plus-core/src/device/r4300/r4300_core.c
index 1234567..abcdefg 100644
--- a/mupen64plus-core/src/device/r4300/r4300_core.c
+++ b/mupen64plus-core/src/device/r4300/r4300_core.c
@@ -64,7 +64,11 @@ void init_r4300(struct r4300_core* r4300, struct memory* mem, struct mi_controlle
     r4300->mi = mi;
     r4300->rdram = rdram;
     r4300->randomize_interrupt = randomize_interrupt;
     r4300->start_address = start_address;
+#ifdef __EMSCRIPTEN__
+    srand(0);
+#else
     srand((unsigned int) time(NULL));
+#endif
 }

 void poweron_r4300(struct r4300_core* r4300)
diff --git a/mupen64plus-core/src/main/main.c b/mupen64plus-core/src/main/main.c
index 1234567..abcdefg 100644
--- a/mupen64plus-core/src/main/main.c
+++ b/mupen64plus-core/src/main/main.c
@@ -1538,7 +1538,11 @@ m64p_error main_run(void)
     }

     /* Seed MPK ID gen using current time */
+#ifdef __EMSCRIPTEN__
+    uint64_t mpk_seed = 0;
+#else
     uint64_t mpk_seed = !netplay_is_init() ? (uint64_t)time(NULL) : 0;
+#endif
     l_mpk_idgen = xoshiro256pp_seed(mpk_seed);

     no_compiled_jump = 0; //ConfigGetParamBool(g_CoreConfig, "NoCompiledJump");
diff --git a/mupen64plus-core/src/device/controllers/paks/biopak.c b/mupen64plus-core/src/device/controllers/paks/biopak.c
index 1234567..abcdefg 100644
--- a/mupen64plus-core/src/device/controllers/paks/biopak.c
+++ b/mupen64plus-core/src/device/controllers/paks/biopak.c
@@ -54,7 +54,11 @@ static void read_biopak(struct biopak* biopak,
     /* Bio sensor returns "heart rate" data based on time. Real hardware
      * reads from an IR sensor; emulation fakes it with time(). */
     {
+#ifdef __EMSCRIPTEN__
+        time_t now = 0;
+#else
         time_t now = time(NULL) * 1000;
+#endif
         uint16_t value;
