diff --git a/Makefile.emulatorjs b/Makefile.emulatorjs
index a626706..47d6ea0 100644
--- a/Makefile.emulatorjs
+++ b/Makefile.emulatorjs
@@ -126,7 +126,13 @@ EXPORTED_FUNCTIONS = _main,_malloc,_free,_load_state, \
                      _system_restart,_cmd_savefiles,_get_core_options,_cmd_save_state,_supports_states,_reset_cheat,_toggleMainLoop, \
                      _save_file_path,_get_disk_count,_set_current_disk,_get_current_disk,_refresh_save_files,_toggle_fastforward,_set_ff_ratio, \
                      _toggle_slow_motion,_set_sm_ratio,_toggle_rewind,_set_rewind_granularity,_get_current_frame_count,_ejs_set_keyboard_enabled, \
-                     _set_vsync,_set_video_rotation,_get_video_dimensions,EmscriptenSendCommand,EmscriptenReceiveCommandReply,_get_memory_data
+                     _set_vsync,_set_video_rotation,_get_video_dimensions,EmscriptenSendCommand,EmscriptenReceiveCommandReply,_get_memory_data, \
+                     _kn_set_frame_time,_kn_set_deterministic,_kn_get_cycle_time_ms, \
+                     _kn_get_audio_ptr,_kn_get_audio_samples,_kn_reset_audio,_kn_get_audio_rate, \
+                     _kn_reset_cycle_count,_kn_canon_fpu_regs, \
+                     _kn_sync_hash,_kn_sync_read,_kn_sync_write,_kn_sync_hash_regions,_kn_rdram_block_hashes, \
+                     _kn_sync_read_regions,_kn_sync_write_regions, \
+                     _kn_frame_hash,_kn_set_skip_rsp_audio
 
 EXPORTS := Browser,callMain,cwrap,getValue,HEAPU8,FS,PATH,ERRNO_CODES,AL,abort,ENV,stringToNewUTF8,UTF8ToString,Browser,EmscriptenSendCommand,EmscriptenReceiveCommandReply,EmulatorJSGetState,EmulatorJSGetMemoryData
 
@@ -222,6 +228,9 @@ endif
 
 ifeq ($(PROXY_TO_PTHREAD), 1)
 else ifeq ($(ASYNC), 1)
+   DEFINES += -DEMSCRIPTEN_AUDIO_EXTERNAL_BLOCK
+   DEFINES += -DEMSCRIPTEN_AUDIO_FAKE_BLOCK
+   DEFINES += -DEMSCRIPTEN_AUDIO_EXTERNAL_WRITE_BLOCK
 else
    DEFINES += -DEMSCRIPTEN_AUDIO_EXTERNAL_BLOCK
    ifeq ($(MIN_ASYNC), 1)
diff --git a/audio/drivers/openal.c b/audio/drivers/openal.c
index be6c1f6..72f263e 100644
--- a/audio/drivers/openal.c
+++ b/audio/drivers/openal.c
@@ -14,6 +14,7 @@
  *  If not, see <http://www.gnu.org/licenses/>.
  */
 
+#include <stdint.h>
 #include <stdlib.h>
 #include <string.h>
 #include <time.h>
@@ -178,6 +179,13 @@ static void *al_init(const char *device, unsigned rate, unsigned latency,
    al->rate  = rate;
    *new_rate = rate;
 
+#ifdef __EMSCRIPTEN__
+   {
+      extern int kn_audio_rate;
+      kn_audio_rate = rate;
+   }
+#endif
+
    if (alIsExtensionPresent("AL_EXT_FLOAT32"))
    {
       al->format      = alGetEnumValue("AL_FORMAT_STEREO_FLOAT32");
@@ -254,6 +262,47 @@ static ssize_t al_write(void *data, const void *s, size_t len)
    const uint8_t *buf = (const uint8_t*)s;
    size_t        _len = 0;
 
+#ifdef __EMSCRIPTEN__
+   {
+      extern int kn_deterministic_mode;
+      if (kn_deterministic_mode)
+      {
+         /* Bypass OpenAL — capture audio to int16 buffer for JS playback.
+          * Data may be float32 or int16 depending on al->format. */
+         extern int16_t kn_audio_buffer[];
+         extern int kn_audio_sample_count;
+         int sample_size = (al->format == AL_FORMAT_STEREO16)
+                           ? (int)sizeof(int16_t) : (int)sizeof(float);
+         int frames = len / (2 * sample_size);
+         int space = 48000 - kn_audio_sample_count;
+         if (frames > space) frames = space;
+         if (frames > 0)
+         {
+            if (al->format == AL_FORMAT_STEREO16)
+            {
+               memcpy(&kn_audio_buffer[kn_audio_sample_count * 2],
+                      buf, frames * 2 * sizeof(int16_t));
+            }
+            else
+            {
+               /* float32 stereo -> int16 stereo */
+               const float *fbuf = (const float *)buf;
+               int i;
+               for (i = 0; i < frames * 2; i++)
+               {
+                  float s = fbuf[i];
+                  if (s > 1.0f) s = 1.0f;
+                  if (s < -1.0f) s = -1.0f;
+                  kn_audio_buffer[kn_audio_sample_count * 2 + i] = (int16_t)(s * 32767.0f);
+               }
+            }
+         }
+         kn_audio_sample_count += frames;
+         return len;
+      }
+   }
+#endif
+
    while (len)
    {
       ALint val;
@@ -309,6 +358,18 @@ static bool al_start(void *data, bool is_shutdown)
 static size_t al_write_avail(void *data)
 {
    al_t *al = (al_t*)data;
+#ifdef __EMSCRIPTEN__
+   /* kaillera-next: In deterministic mode, still unqueue buffers (so OpenAL
+    * al_write() bypasses OpenAL entirely, so no buffers are queued.
+    * Skip al_unqueue_buffers and report fixed space. */
+   {
+      extern int kn_deterministic_mode;
+      if (kn_deterministic_mode)
+      {
+         return OPENAL_BUFSIZE * 4;
+      }
+   }
+#endif
    al_unqueue_buffers(al);
    return al->res_ptr * OPENAL_BUFSIZE;
 }
diff --git a/frontend/drivers/platform_emulatorjs.c b/frontend/drivers/platform_emulatorjs.c
index c9ceed1..d66e86b 100644
--- a/frontend/drivers/platform_emulatorjs.c
+++ b/frontend/drivers/platform_emulatorjs.c
@@ -18,6 +18,7 @@
 #include <emscripten/emscripten.h>
 #include <emscripten/html5.h>
 #include <emscripten/threading.h>
+#include <stdint.h>
 #include <string.h>
 #include <malloc.h>
 #include <stdio.h>
@@ -115,6 +116,41 @@ typedef struct
 
 static emscripten_platform_data_t *emscripten_platform_data = NULL;
 
+/* kaillera-next: Deterministic timing for lockstep netplay.
+ * When kn_deterministic_mode is non-zero, all timing functions return
+ * values derived from the N64 CPU cycle counter (CP0 Count register)
+ * instead of real wall-clock time. This is deterministic because both
+ * emulators execute the same instructions and have the same cycle count.
+ *
+ * kn_get_cycle_time_ms() reads the CP0 Count register and converts it
+ * to milliseconds. This is what Project64k/Kaillera effectively did —
+ * timing was derived from CPU cycles, not wall clock. */
+int kn_deterministic_mode = 0;
+double kn_frame_time_ms = 0.0;
+
+/* kaillera-next: Audio capture buffer for deterministic audio bypass.
+ * When kn_deterministic_mode is on, al_write() copies PCM samples here
+ * instead of sending to OpenAL. JS reads after each frame step. */
+#define KN_AUDIO_BUF_SAMPLES 48000
+int16_t kn_audio_buffer[KN_AUDIO_BUF_SAMPLES * 2]; /* stereo interleaved int16 */
+int kn_audio_sample_count = 0;
+int kn_audio_rate = 0;  /* set by al_init() */
+
+EMSCRIPTEN_KEEPALIVE void* kn_get_audio_ptr(void)    { return kn_audio_buffer; }
+EMSCRIPTEN_KEEPALIVE int   kn_get_audio_samples(void) { return kn_audio_sample_count; }
+EMSCRIPTEN_KEEPALIVE void  kn_reset_audio(void)       { kn_audio_sample_count = 0; }
+EMSCRIPTEN_KEEPALIVE int   kn_get_audio_rate(void)    { return kn_audio_rate; }
+
+EMSCRIPTEN_KEEPALIVE void kn_set_frame_time(double time_ms)
+{
+   kn_frame_time_ms = time_ms;
+}
+
+EMSCRIPTEN_KEEPALIVE void kn_set_deterministic(int enable)
+{
+   kn_deterministic_mode = enable;
+}
+
 /* defined here since implementation is frontend dependent */
 
 void retro_sleep(unsigned msec)
diff --git a/libretro-common/features/features_cpu.c b/libretro-common/features/features_cpu.c
index a4f92f2..328c952 100644
--- a/libretro-common/features/features_cpu.c
+++ b/libretro-common/features/features_cpu.c
@@ -211,7 +211,15 @@ retro_perf_tick_t cpu_features_get_perf_counter(void)
 #elif defined(HAVE_LIBNX)
    time_ticks = armGetSystemTick();
 #elif defined(EMSCRIPTEN)
-   time_ticks = emscripten_get_now() * 1000;
+   {
+      /* kaillera-next: use deterministic frame time in lockstep mode */
+      extern int kn_deterministic_mode;
+      extern double kn_frame_time_ms;
+      if (kn_deterministic_mode)
+         time_ticks = (retro_perf_tick_t)(kn_frame_time_ms * 1000);
+      else
+         time_ticks = emscripten_get_now() * 1000;
+   }
 #endif
 
    return time_ticks;
@@ -248,7 +256,14 @@ retro_time_t cpu_features_get_time_usec(void)
       return 0;
    return tv.tv_sec * INT64_C(1000000) + (tv.tv_nsec + 500) / 1000;
 #elif defined(EMSCRIPTEN)
-   return emscripten_get_now() * 1000;
+   {
+      /* kaillera-next: use deterministic frame time in lockstep mode */
+      extern int kn_deterministic_mode;
+      extern double kn_frame_time_ms;
+      if (kn_deterministic_mode)
+         return (retro_time_t)(kn_frame_time_ms * 1000);
+      return emscripten_get_now() * 1000;
+   }
 #elif defined(PS2)
    return ps2_clock() / PS2_CLOCKS_PER_MSEC * 1000;
 #elif defined(VITA) || defined(PSP)
