version: '3.8'

services:
  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'
    volumes:
      - redis-dev-data:/data
    command: redis-server --save 60 1 --loglevel warning

volumes:
  redis-dev-data:
