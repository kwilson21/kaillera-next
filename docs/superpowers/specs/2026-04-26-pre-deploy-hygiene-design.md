# Pre-deploy hygiene, doc refresh & commit split

**Date:** 2026-04-26
**Status:** Approved (pending spec review)
**Scope:** Hygiene-only (no structural refactor)

## Goal

Take the working tree from its current state (14 modified files + ~30 untracked
items + stale docs) to a clean, deployable `main` with semantically split
commits and freshened documentation. **No structural refactor of monolith
files.** The deploy itself (`just deploy`) is out of scope — this prep pass
ends at a clean tree on `main` that the user can then deploy.

## Non-goals

- Refactoring `netplay-lockstep.js` (9,518 lines) or `play.js` (4,590 lines).
  Decomposition is tracked separately in `project_monolith_decomposition.md`
  and deserves its own session, not a pre-deploy bundle.
- Running `just deploy` or any deploy step. The user does the Playwright
  two-tab smoke and triggers deploy themselves.
- Editing `CHANGELOG.md`. It is regenerated by `scripts/bump-version.sh` when
  `just deploy` runs (per `reference_deploy_workflow.md`).
- Wiring SoftFloat conversion files or FPU rounding patch into the prod build.
  These are speculative paths not currently compiled into `libsoftfloat.a`;
  wiring them would require a fresh WASM build and full validation cycle.
- Adding new tests. Per `feedback_test_what_fixes_desyncs`, hygiene/diagnostic
  improvements do not require a new test run.

## Sequencing

The order is chosen so each commit is small enough to verify on its own, and
so later commits don't have to step around earlier untracked files.

### Pre-step (no commit) — delete scratch

A single `rm` batch, reviewable file-by-file before execution:

- `round3-log.json`, `round3-postfix-log.json`
- `web/static/ejs/cores/mupen64plus_next-wasm.data.bak-20260424-1245`
- `web/static/ejs/cores/mupen64plus_next-wasm.data.bak-8927a38a`
- `tools/deepseek_screenshot_diff.py` (dead-end path; documented as such)
- `tests/recomp-ci/` (whole dir — mixed binaries, dSYM, screenshots, logs;
  useful scripts were tied to deleted sandboxes)
- `build/patch-asyncify-counter.py` (keep `.mjs`)
- `build/patch-fpu-rounding.py`
- `build/calc_ft_mask.py`
- The 8 untracked SoftFloat conversion `.c` files in `build/softfloat/`:
  `f32_to_i32.c`, `f32_to_i32_r_minMag.c`, `f32_to_i64.c`,
  `f32_to_i64_r_minMag.c`, `f64_to_i32.c`, `f64_to_i32_r_minMag.c`,
  `f64_to_i64.c`, `f64_to_i64_r_minMag.c`

Rationale for deletes: not in production build (`build/build.sh` SF_SOURCES
list at lines 517–584 does not include them), recoverable from git reflog or
this conversation if the speculative path is later resurrected.

### Commit 1 — `chore: gitignore local sandboxes & generated artifacts`

`.gitignore` additions:

- `server/kaillera.db` (local SQLite)
- `build/build/`, `build/recomp/`, `build/recomp-tool/`, `build/test/`
- `tools/dl-recorder/`
- `web/smash64r-test/`, `web/webgpu-pivot-test/`

Only `.gitignore` is staged. After this commit, `git status` shows only the
files we intend to commit in subsequent steps.

### Commit 2 — `feat(rollback): add menu-start barrier and C-input pipeline`

Lead the commit body with the actual sync fix; supporting work is mentioned
after.

**Primary fix (sync correctness):**

- `web/static/netplay-lockstep.js`:
  - Menu-start barrier: settle delay (`MENU_START_BARRIER_SETTLE_MS = 500`),
    per-peer phase broadcast (`PHASE_BROADCAST_INTERVAL_MS`,
    `PHASE_STALE_MS`), local/peer ready tracking
  - C-input pipeline: `_feedCInput` direct feed plus `_backfillCInputsFromJs`
    rolling-window backfill on rollback restore
  - Rollback delay clamps (`ROLLBACK_MIN_DELAY_FRAMES = 4`,
    `ROLLBACK_MAX_DELAY_FRAMES = 7`)
  - Guest-state kind tracking (`'savestate'` vs `'kn-sync'`) and host-side
    hidden-state sidecar
  - Input-buttons normalization (`_inputButtons`, `_cloneInput`)

**Supporting work:**

- Field-granular hash registry: `build/kn_rollback/kn_hash_registry.c`,
  `build/kn_rollback/kn_rollback.c`, `build/build.sh`
- Cross-peer detector wiring: `web/static/kn-desync-detector.js`,
  `web/static/kn-diagnostics.js`, `web/static/kn-state.js`,
  `web/static/play.js`, `web/static/shared.js`, `web/play.html`
- Audio determinism patch: `web/static/kn-audio.js`,
  `build/patches/audio-backend-skip-output.patch` (verified wired into
  `build.sh:165–168`)
- Server payload/event additions: `server/src/api/payloads.py`,
  `server/src/api/signaling.py`

**Deployed core trio (committed together as a unit since `.data` is already
tracked):**

- `web/static/ejs/cores/mupen64plus_next-wasm.data`
- `web/static/ejs/cores/mupen64plus_next_libretro.js`
- `web/static/ejs/cores/mupen64plus_next_libretro.wasm`

URL-param diagnostic flags (`knperf=light`, `kntrace=1`, `kndiag=deep`,
`desync=deep`, `knflush=live`) are gated behind opt-in query strings and ship
as part of this commit.

### Commit 3 — `docs: refresh project + module documentation for prod cut`

Two passes — content freshening, then mechanical sync of headers/docstrings.
See "Doc-refresh scope" section below for the per-file plan.

### Commit 4 — `test: determinism automation harness`

Test-infrastructure scaffolding only; not run as part of this pass.

- `tests/determinism-automation.mjs`
- `tests/record-nav.mjs`, `tests/record-nav-auto.mjs`
- `tests/fixtures/nav-recording.json`
- `tests/package.json`

### Commit 5 — `chore(diagnostics): research patches + tools (not in prod build)`

Non-runtime, non-build-input research artifacts kept for future investigation.

**Diagnostic patches (NOT applied by `build.sh`):**

- `build/patches/mupen64plus-defer-video.patch` (Option Y — future use)
- `build/patches/mupen64plus-interrupt-counters.patch`
- `build/patches/rdram-det-watch.patch`

**Diagnostic scripts:**

- `build/patch-asyncify-counter.mjs` (Option Z diagnostic)

**Diagnostic tools (`tools/`):**

- `composite_grid.py`, `composite_screenshots.py` (visual cross-peer diff)
- `diff_rand_calls.py`, `diff_thread_samples.py` (RNG / thread divergence)
- `pc_to_symbol.py` (PC→symbol resolver)
- `rdram_diff.py` (RDRAM region comparison)
- `trace_extract.py`, `trace_generate.py` (training-data tooling for vision
  pipeline; classified as research, not runtime/build/test infra)

After commit 5, `git status --porcelain` is empty.

## Doc-refresh scope (commit 3)

Two passes.

### Content pass — verify and minimally update

For each file, read it against current code and either: (a) leave unchanged
if accurate, (b) update specific lines that drifted, or (c) prepend a
historical note rather than rewrite if it has drifted too far.

- **`CLAUDE.md`** — verify file/feature inventory matches reality. Specifically
  check: the netplay file inventory under "Repo structure", the V1 scope
  table, the Socket.IO events table (verify against `server/src/api/payloads.py`
  and `server/src/api/signaling.py`), the netplay-invariants link target.
- **`README.md`** — verify install/run instructions, port (27888), env vars
  (`ALLOWED_ORIGIN`, `MAX_ROOMS`, `MAX_SPECTATORS`), feature highlights.
- **`CONTRIBUTING.md`** — verify tooling references (`uv` per
  `feedback_uv_python_tooling`, `just` recipes per
  `reference_deploy_workflow`).
- **`docs/roadmap.md`** — header asserts "All V1 phases complete"; verify V1
  feature list matches CLAUDE.md V1 scope table; update V2 status if anything
  has moved.
- **`docs/mvp-plan.md`** — likely stale (predates V1 ship). Recommend:
  prepend "**Historical — see `docs/roadmap.md` for current state**" rather
  than rewrite.
- **`docs/netplay-invariants.md`** — verify I1/I2/R1–R6 still match the
  lockstep code; add the menu-start barrier as part of the I1 deadline-site
  list if it qualifies.

### Mechanical pass — module headers and docstrings

- Top-of-file header comments in `web/static/*.js` — update purpose lines
  that drifted; check that referenced collaborators still exist.
- Module docstrings in `server/src/**/*.py` — same treatment. Verify
  `server/src/api/desync_prompts.py` and `server/src/api/desync_vision.py`
  have accurate module docstrings (both files confirmed present in tree at
  spec-write time).
- **No new comments added** for code that didn't have them. Per CLAUDE.md
  ("default to writing no comments"), only freshen existing comments that
  have drifted.

### Out of scope for doc refresh

- `CHANGELOG.md` (auto-generated by deploy script; never hand-edit)
- `docs/superpowers/{plans,sessions,specs}/` (planning artifacts, not user
  docs)

## Verification gates

After each commit, before moving to the next:

1. `git status` shows only files expected to still be unstaged.
2. `git diff HEAD~1 --stat` matches the commit's intended scope.

**Commit 2 specifically (the only one touching runtime code):** No new long
stress run is required in this hygiene pass; cite the already-run V8/JSC
5-minute stress pass, and run cheap syntax checks if staging changes make
that practical. The runtime fix was validated before this commit-split pass
began.

**Commit 3 specifically:** `git diff HEAD~1` for any `*.md`, `*.py`, or `*.js`
should show only comment-block / markdown changes — no semantic source
edits leaked into the docs commit.

**Final gate:** `git status --porcelain` returns empty.

## Risks & mitigations

| Risk | Mitigation |
|---|---|
| Doc-refresh accidentally edits source code | Commit 3 verification gate; restrict edits to comment blocks + `.md` files |
| WASM trio on `main` without matching patch sources | Already mitigated — `audio-backend-skip-output.patch` is the only prod build input added in commit 2; SoftFloat scaffolding upstream is unchanged |
| Hidden untracked file gets dropped | Pre-step `rm` batch is explicit and reviewable; no glob-based deletions |
| `git status` not actually clean at end | Final gate: `git status --porcelain` returns empty before handoff |
| Doc rewrites inflate scope | Content pass uses "verify and minimally update" wording; if a doc is too far gone, prepend historical note rather than rewrite |
| Module header rewrite invents APIs | Only freshen existing comments; verify referenced files/symbols exist (`grep`) before citing them |

## Handoff

After commit 5, the user runs:

```
just deploy-dry   # preview unpushed commits + version bump
# Playwright two-tab smoke (per feedback_playwright_before_deploy)
just deploy       # bump + push + tag
```

The deploy script generates one `chore(version)` commit with bullets for
the substantive commits in this pass.
